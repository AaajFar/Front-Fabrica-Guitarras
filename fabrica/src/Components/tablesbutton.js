import React from "react";
import 'bootstrap/dist/css/bootstrap.css';

const tableButton = ({ titulo }) => {
    return (
        <button>
            {titulo}
        </button>
    );
}

export default tableButton;